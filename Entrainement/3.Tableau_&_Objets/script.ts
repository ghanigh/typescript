const fruits = ["orange", "pomme"]
fruits.push("fraise")

console.log(fruits);

const mixe = [1,'str', [1, 2, 3 ]]

/*
La variable va recevoir un tableau mais le tableau n'existe pas 

*/
let array: number [] 
// array.push(2) // erreur
array = [1, 2, 4,]

let array2: number[] = [] // Le tableau est initialisé
array2.push(2)

let random: any [];
random = [true, false, 1, "ghani"]

// Objets

const hero = {
    name: "Deku",
    stamina: 100000,
    speed: 103
}
let user : {
    name: string,
    stamina: number,
    passions:[]
}

